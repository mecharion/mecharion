# 由 Mecharion 渲染，请勿手工编辑
# 与 exports.client 产出的是同一个串——本模板供本 Component 内的 client 角色使用，
# exports 供**其他 Pack**（hdfs / kafka）引用。
ZK_CONNECT={{ range $i, $n := .Topology.Role "server" }}{{ if $i }},{{ end }}{{ $n.Address }}:{{ $.Params.client_port }}{{ end }}
