# 由 Mecharion 渲染，请勿手工编辑
#
# JAVA_HOME 来自 scope: node 的依赖，而非约定路径——
# jdk11 的 Component 名不一定叫 "jdk11"。
export JAVA_HOME="{{ .Requires.jdk11.Paths.Current }}"
export JVMFLAGS="-Xms{{ .Params.heap }} -Xmx{{ .Params.heap }}"
export ZOO_LOG_DIR="{{ .Paths.Logs }}"
