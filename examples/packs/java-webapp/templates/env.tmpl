# 由 Mecharion 渲染，请勿手工编辑。
#
# 这个文件是**唯一**含口令的地方，权限 0640。主配置里只有一个
# ${DB_PASSWORD} 占位，因此主配置可以随手复制进工单或支持包而不泄密。
#
# systemd 按 KEY=value 逐行解析本文件（EnvironmentFile=），不做 shell 展开。
# 口令由 postgresql 的 generate 产生，默认字符集是 alnum——正是为了让它
# 能安全穿过这一层与后面的各种解析器，不必操心转义。
DB_PASSWORD={{ .Params.db_password }}
