# 已确认的误报，不是真实泄漏——见每行前一次 gitleaks 扫描时的复核记录。
#
# docs/design/08-security.md 里展示的初始 admin token 示例，以字面量
# "..." 结尾，是说明格式用的占位符，不是真实凭据。指纹里带 commit
# hash——发布前把历史压成一个提交时这个指纹会跟着变，届时要重新跑
# 一次 `make gitleaks` 换上新的指纹，不能指望旧指纹继续生效。
32c2979fb952a4055be910b8997462e7c3d4147d:docs/design/08-security.md:generic-api-key:202
